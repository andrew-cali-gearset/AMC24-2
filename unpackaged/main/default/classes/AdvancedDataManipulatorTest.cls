@isTest public class AdvancedDataManipulatorTest {
   	/* This method is called when the test is run. */
    @IsTest(SeeAllData=true) public static void AdvancedDataManipulatorTest() {
        // Instantiate a new controller with all parameters in the page
        AdvancedDataManipulator controller = new AdvancedDataManipulator();
      }
}